name: ⭐ Create release
on:
  push:
permissions:
  contents: write
  pull-requests: write
jobs:
  merge:
    runs-on: ubuntu-22.04
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v3
      - uses: VelcoTech/github-workflows/.github/actions/json-to-env@master
        with:
          json: '{"a": "a"}'
      - uses: VelcoTech/github-workflows/.github/actions/json-to-env@master
        with:
          json: '{"b": "b"}'
      - run: printenv
