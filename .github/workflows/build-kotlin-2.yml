name: 📦 Reusable
run-name: 📦️ Reusable kotlin - ${{ github.event.repository.name }} by @${{ github.actor }}
on:
  workflow_call:
jobs:
  reusable:
    name: Reusable
    runs-on: ubuntu-22.04
    steps:
      - name: ⬇️ Checks-out code
        uses: actions/checkout@v3
